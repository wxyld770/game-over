# Add as a systemd drop-in for game-over.service.
# Store METRICS_TOKEN and METRICS_FILE in a root-owned 0600 EnvironmentFile.
[Service]
StateDirectory=game-over
EnvironmentFile=/etc/game-over/metrics.env
